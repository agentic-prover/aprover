{
  "saved_at": "2026-05-21T12:11:56.481344+00:00",
  "spec": {
    "function_name": "npid_get_allocated_memory",
    "precondition": "requires valid(nd) && valid(host_memory) && valid(device_memory)",
    "postcondition": "(ensures (result == 0 ==> (*host_memory >= 0 && *device_memory >= 0)) && (result == -1 ==> (*host_memory == 0 && *device_memory == 0))) && (result == (npid_find_process_slot(nd) == -1 ? -1 : 0) && (result == 0 ==> (*host_memory == nd->attached_processes[npid_find_process_slot(nd)].memory_used[MEM_LOC_HOST - 1] && *device_memory == nd->attached_processes[npid_find_process_slot(nd)].memory_used[MEM_LOC_DEVICE - 1])) && (result == -1 ==> (*host_memory == 0 && *device_memory == 0)))",
    "callee_specs": {},
    "loop_invariants": [],
    "status": "generated",
    "spec_disagreement": false
  }
}