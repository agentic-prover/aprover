{
  "saved_at": "2026-05-21T12:05:52.825045+00:00",
  "spec": {
    "function_name": "npid_attached_process_count",
    "precondition": "requires valid(nd) && valid_range(nd->attached_processes, 0, 16)",
    "postcondition": "(ensures result >= 0 && result <= 16 && result == (number of indices i in [0,16) where nd->attached_processes[i].pid != 0)) && (result == ({ int c = 0; for (int i = 0; i < 16; i++) { if (nd->attached_processes[i].pid != 0) c++; } c; }))",
    "callee_specs": {},
    "loop_invariants": [],
    "status": "generated",
    "spec_disagreement": false
  }
}